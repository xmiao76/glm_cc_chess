# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

`glm_cc_chess` is a Windows desktop chess application with a Pygame GUI and a built-in chess engine. A human can play against the engine, watch engine-vs-engine games, or let the engine play against opponents on Lichess via a dedicated BOT account (live board display + game review). The engine is also exposed through a UCI entry point.

## Tech Stack

- **Language**: Python 3.12+
- **GUI**: Pygame
- **Testing**: pytest + pytest-cov
- **Engine**: Custom minimax with alpha-beta pruning, iterative deepening, quiescence search

## Project Structure

```
src/
  board.py             — Board representation, FEN parsing, piece operations
  moves.py             — Move generation, legality, SAN + UCI move conversion
  game.py              — Game state controller, make/unmake, draw detection
  engine.py            — Chess engine (minimax + alpha-beta + quiescence) + choose_move()
  gui.py               — Pygame GUI (board, panel, Lichess mode, review)
  gui_textinput.py     — Reusable single-line text input field (Lichess opponent + masked token entry; mask, insert_text/paste, display_text)
  clipboard_util.py    — System clipboard copy (Copy Log) + paste (token-field Ctrl+V); stdlib backends
  uci.py               — UCI protocol entry point (python -m src.uci)
  lichess_client.py    — Lichess BOT API HTTP client (stdlib urllib)
  lichess_controller.py— Threading + queue bridge between API streams and GUI
  main.py              — GUI entry point
lichess/
  config.yml.example   — lichess-bot config template (optional headless bridge)
  run_engine.bat       — Windows wrapper that runs `python -m src.uci`
  README_LICHESS.md    — Lichess integration guide
tests/
  test_board.py
  test_moves.py
  test_game.py
  test_engine.py
  test_uci.py
  test_lichess.py
```

## Commands

```bash
# Setup
python -m venv venv
source venv/Scripts/activate    # Windows (Git Bash)
pip install -e ".[dev]"

# Run the application
python -m src.main

# Run the UCI engine (for lichess-bot / any UCI host)
python -m src.uci

# Run all tests
pytest tests/ -v

# Run with coverage
pytest tests/ --cov=src --cov-report=term-missing

# Package the Windows release
python package.py
```

## Architecture

- **Board** (`src/board.py`): 8x8 array with piece codes (`wP`, `bK`, etc.), FEN parsing/generation
- **Moves** (`src/moves.py`): Pseudo-legal and legal move generation, check/checkmate/stalemate detection, SAN (`move_to_algebraic`) and UCI (`move_to_uci`/`uci_to_move`) conversion
- **Game** (`src/game.py`): Full game state with make/unmake, castling rights, en passant, draw rules
- **Engine** (`src/engine.py`): Evaluation with piece-square tables, search with alpha-beta + quiescence; `choose_move(board, time_limit_ms=None, max_depth=None)` is the clean interface shared by GUI/UCI/Lichess
- **GUI** (`src/gui.py`): Pygame board rendering, click-to-move, legal move highlighting, side panel; "AI vs Lichess" mode + game review
- **UCI** (`src/uci.py`): UCI protocol stdin/stdout loop (`python -m src.uci`); stdout is pure UCI, debug to stderr
- **LichessClient** (`src/lichess_client.py`): Lichess BOT API over stdlib `urllib`; NDJSON streaming; token via `Authorization: Bearer` header (never in URLs/logs)
- **LichessController** (`src/lichess_controller.py`): Background daemon threads for event/game streams; pushes typed events onto a `queue.Queue`; the pygame main loop drains it. Background threads never call pygame.

## Lichess Integration

- Primary: in-GUI "AI vs Lichess" mode (live board + move-by-move review).
- The bot can also **initiate** games: a manual **Challenge** (target username)
  and an **Auto** toggle (auto-accept peers + periodic auto-challenge). Two
  programs with Auto on auto-match via a leader/follower rule (the
  alphabetically-earlier bot challenges) so exactly one game starts.
- Optional: `lichess-bot` headless bridge via the UCI entry point (`lichess/config.yml.example`, `lichess/run_engine.bat`).
- Token: `LICHESS_BOT_TOKEN` env var or gitignored `lichess/config.yml`. Never hardcoded/committed.
- In-GUI token entry: the Lichess panel shows a **masked** token input + a **Connect** button while not connected, so the user can paste/type the token in the UI instead of setting the env var first. On Connect the GUI sets `os.environ["LICHESS_BOT_TOKEN"]` (so the rest of the codebase, which reads that env var, sees it) and starts the controller; a token already in the env/config auto-connects on entering Lichess mode (backward-compatible). The token is a secret: it is never logged, never written to a file, and the field renders one `*` per char (`TextInput(mask=True)` + `display_text()`; `*` not `•` because the bundled default font lacks U+2022). Ctrl+V pastes from the clipboard into the focused field via `clipboard_util.paste_from_clipboard` (pygame scrap → tkinter), filtered to token-legal chars (`_LICHESS_TOKEN_CHARS = [a-zA-Z0-9_]`) so a trailing newline/space from copying doesn't corrupt the token; Enter in the token field also Connects. The field cap `_LICHESS_TOKEN_MAX_LEN = 1024` follows the Lichess OpenAPI spec (tokens are `^[A-Za-z0-9_]+$` — NO hyphen — and "make sure your application can handle at least 512 characters", can grow without notice): a prior 128 cap TRUNCATED >128-char tokens, so the controller sent a partial token and Lichess returned HTTP 401 "No such token" → `Error: profile fetch failed: ... HTTP 401` (the user's "Profile Fetch Error" on every Connect); 1024 covers the spec's 512 with headroom. (SSL is NOT the issue in the frozen EXE — a frozen `urllib` HTTPS probe returns 401, not an `[SSL: ...]` URLError; on Windows `ssl.create_default_context()` loads the OS cert store, which works frozen.) A SECOND, separate cause affected SHORT valid tokens (the truncation fix alone did not resolve the user's `lip_...` token): on a Windows/IME setup `KEYDOWN.unicode` arrives empty and the OS-composed char arrives as a separate `TEXTINPUT` event; the old key-name fallback (`pygame.key.name(K_MINUS)` is `-`, not token-legal) DROPPED `_`, mangling `lip_X` → `lipX` → the same HTTP 401 "No such token". Fixed by routing `pygame.TEXTINPUT` (OS-composed, IME/shift-correct) to the focused field's `insert_text` (`ChessGUI._handle_lichess_textinput` + a `TEXTINPUT` branch in `run()`), with `handle_key` inserting from `KEYDOWN.unicode` only and a `_keydown_handled_char` dedup flag so the non-IME path (KEYDOWN inserts) and the IME path (TEXTINPUT inserts) don't double-insert; the key-name fallback (`_char_from_event`) is removed (do NOT restore it — it drops `_`). Proven frozen via a one-file probe (`FROZEN_TEXTINPUT_OK=True` for `lip_BXsJDkm4DGXnoDrzrVEl`). A THIRD cause — the TEXTINPUT-only fix was INSUFFICIENT for the user's exact IME: their IME fires TEXTINPUT for letters/digits but NOT for the underscore (Shift+hyphen), so the TEXTINPUT-only path STILL dropped `_` (`lip_xxxxx` → `lipxxxx`) → the same HTTP 401. (The `FROZEN_TEXTINPUT_OK=True` probe tested the case where TEXTINPUT DOES fire for `_`, which is NOT the user's IME — so it passed without catching this.) PROVEN by real-API reproduction (`LichessClient(lip_BXsJDkm4DGXnoDrzrVEl).get_profile()` → HTTP 200, profile `xmiao_ds`, title `BOT`) — the token is valid, so the 401 was the mangled token. Fixed by a KEYDOWN fallback in `handle_key`: when `KEYDOWN.unicode` is empty, capture NON-composing keys (shifted symbols via `_SHIFTED_SYMBOLS` e.g. `K_MINUS`+Shift→`_`; digits via `_DIGIT_KEYS`) from `event.key` + `event.mod` (`_fallback_char`), setting `_keydown_handled_char=True` so a later TEXTINPUT (if it does fire) skips (dedup). Letters have NO fallback (rely on TEXTINPUT — a key-name letter would corrupt a composing-IME field). Populated-but-invalid unicode is REJECTED, never substituted (`!` on `K_1` is rejected, not turned into `1`). TDD: `test_underscore_captured_when_ime_skips_textinput_for_it` (the user's exact no-TEXTINPUT-for-`_` scenario), `test_fallback_captures_underscore_without_textinput`, `test_fallback_captures_digit_without_textinput`, `test_populated_but_invalid_unicode_does_not_fallback`. Proven frozen (`FROZEN_UNDERSCORE_OK=True`, `FIELD_VALUE='lip_BXsJDkm4DGXnoDrzrVEl'` for the no-TEXTINPUT-for-`_` scenario). A FOURTH cause — the one that ACTUALLY resolved the user's recurring bug (causes 1-3 were real fixes but not THIS user's issue): the round-3 fallback covered `_`/digits but NOT letters, relying on TEXTINPUT for letters; the user's IME skips TEXTINPUT for uppercase/Shift+letter, so all 10 uppercase letters in `lip_BXsJDkm4DGXnoDrzrVEl` were dropped → mangled to `lip_skm4norzrl` → the same HTTP 401. PROVEN by an END-TO-END multi-IME-model repro (`_diag/repro_capture.py`, since removed) that hit the REAL API per model: the `unshifted_only` model (no TEXTINPUT for uppercase/`_`) and the `none` model (no TEXTINPUT at all) both mangled → 401, while the `no_underscore` model the round-3 probe used MATCHed → 200 (why it passed without catching this). The decisive narrowing came from the USER: a different token works in my app + this token works in another app ⇒ token valid, app works, bug isolated to my-app+this-token. Fixed by adding a LETTER fallback to `_fallback_char` (`_LETTER_KEYS` + Shift/CapsLock XOR; `K_b`+Shift→`B`, Caps→`B`, Shift+Caps→`b`); letters are safe to derive from the key because the token/username fields are ASCII-only (`valid_chars` filters the rest) and the dedup flag prevents double-insert when TEXTINPUT also fires. TDD (`test_typing_token_survives_when_ime_skips_all_textinput` = pure KEYDOWN, NO TEXTINPUT, full token; +4 unit letter tests) + frozen-proven IN-BINARY (a bundled repro EXE: all 4 IME models → MATCH → 200, incl. the harshest no-TEXTINPUT-at-all). Release EXE rebuilt v1.2.0. If a 401 recurs for a short token, have the user Ctrl+V paste (paste bypasses the IME/KEYDOWN path) as a one-shot diagnostic. The token form replaces the opponent controls pre-connection (you can't challenge without a connection); the opponent field is only shown/focusable once connected with no active game and no pending challenge.
- Auto-match env vars: `LICHESS_OPPONENT`, `LICHESS_AUTO_MATCH`, `LICHESS_CLOCK_LIMIT`, `LICHESS_CLOCK_INCREMENT`, `LICHESS_RATED`.
- Dedicated BOT account only; no browser automation; no normal-account automation. See `lichess/README_LICHESS.md`.
- Bot-account requirement: the bot-only endpoints (`/api/bot/game/stream/*`, make-move) return HTTP 400 `"This endpoint can only be used with a Bot account"` for a normal account. The controller detects this at startup via the profile `title == "BOT"` (`is_bot`), blocks challenges and auto-match until True, and exposes `upgrade_account()` (POST `/api/bot/account/upgrade`, irreversible); the GUI shows an "Upgrade to Bot" button when `is_bot` is False.

## Key Design Decisions

- Board uses row 0 = rank 8 (black side), row 7 = rank 1 (white side)
- Moves are tuples: `(from_row, from_col, to_row, to_col, promotion_piece_or_None)`
- Engine evaluates from white's perspective (positive = white advantage)
- Threefold repetition compares position-only FEN (excludes move counters)
- UCI castling is the king's from-to square (non-Chess960); en passant is plain from-to; `GameState.make_move` infers castling/EP/promotion from piece type + squares
- Lichess turn parity: bot (white) moves on even move count, bot (black) on odd; the board is always rebuilt from `initialFen` + the full UCI move list (each `gameState.moves` is authoritative and complete)
- Lichess challenge events carry a `direction` (`in`/`out`): incoming may be auto-accepted (only from configured peers, only when idle); outgoing are tracked in `_pending_outgoing` and canceled before reissue, and on `gameStart` via `_cancel_all_pending` — EXCEPT the entry whose id == the active game id (canceling that one would abort our own just-started 0-move game; see the PROVEN-CAUSE bullet). `_cancel_safely` enforces this exception for every call site
- Lichess auto-match uses a leader/follower rule: the bot challenges only peers whose username sorts after its own, so two identical auto-matching bots create exactly one game (no double games); concurrency is one game at a time (`_active_games`)
- Lichess auto-accept declines `correspondence` challenges from configured peers with reason `tooSlow` (the streaming bot can't safely resume a multi-day game after a restart — the event stream only delivers NEW gameStarts, not in-progress games); real-time speeds (blitz/bullet/rapid/classical) are auto-accepted. Non-peer or Auto-off correspondence still surfaces as `ChallengeReceived` for a manual choice. The GUI logs "Challenged <opp>" once, from the `ChallengeSent` event (single source — `_challenge_opponent` does not log it directly), and includes Lichess's challenge `id=` so one-unique-id-logged-once proves we sent exactly ONE challenge. NOTE: the opponent receiving it twice is SUFFICIENT but NOT NECESSARY evidence of two LIVE event streams on the opponent's account (Lichess delivers each event to every live stream) — the gameStart (not the challenge) is what double-connects the game stream, so a duplicate aborts at creation even with SINGLE challenge receipt (the 2nd stream came up after the challenge but caught the gameStart). Two LIVE streams can be one process with a stale/lingering reconnect stream, not necessarily two processes. Two `Connected as <opp>` lines do NOT prove a duplicate — lichess-bot logs 'Connected' on each reconnect (`start()` re-runs when the event stream drops), so two lines can be ONE process. The decisive check is the bot PROCESS count on BOTH machines (Task Manager), not the `Connected` line count and not the receipt count
- Lichess instant-at-creation abort — PROVEN ROOT CAUSE (2026-06-28; supersedes the same-IP / same-owner / duplicate-stream / no-first-move-timeout / Experiment A / rated-vs-casual candidates below, all now RETRACTED): when our outgoing challenge is accepted, Lichess REUSES the challenge id as the game id (lichess-org/api issue #234 confirms the same id fires for `challengeCanceled` and a later `gameStart`), so `gameStart` arrives for that SAME id. On `gameStart` the CHALLENGER's `_cancel_all_pending()` (in `src/lichess_controller.py`) POSTed `/api/challenge/{id}/cancel` on the pending-outgoing entry whose id == the new game id — i.e. on our OWN just-accepted 0-move game — which Lichess honors as the challenger withdrawing from their own 0-move game and ABORTS it at creation (0 moves, <1s). The ACCEPTOR never cancels (it has no pending outgoing), so only bot-vs-bot — both sides running this code — aborted; a human web game between the same two accounts on the same two machines played fine (user-verified 2026-06-28), proving the abort is this cancel call, NOT a Lichess/owner/IP policy. The earlier "open the game stream first" reorder did NOT stop the aborts (the cancel still ran and still aborted). FIX: `_cancel_safely` now skips any challenge id that is in `self._active_games`, so `_cancel_all_pending()` on `gameStart` only cancels OTHER pending challenges (one-game-at-a-time housekeeping — still verified by `test_gamestart_cancels_other_pending_but_not_the_active_game`) and never the one that became the active game. TDD-pinned by `test_gamestart_does_not_cancel_challenge_that_became_the_game`. With the cancel no longer aborting the game, the instant opening-book move 1 (Experiment A) is no longer needed to beat the abort (harmless to keep as a fast move-1/diagnostic). The observability added during the investigation (abort-diagnostic headline, gameFull-status-on-arrival LIVE/DEAD, game-stream OPEN/CLOSE counter, per-account singleton lock + PID, rated/casual logging) remains useful and is kept; only the causal claims are retracted
- Lichess activity log format: every line is prefixed with a wall-clock `HH:MM:SS` timestamp (`time.strftime`, matching the opponent lichess-bot's format) so the two logs can be correlated and the same-second "instant abort at creation" is visible; the rolling history is 100 lines (Copy Log captures the full game sequence — the challenge line survives; the on-screen panel renders only the last ~6, truncated to 30 chars). The timestamp is the evidence that distinguishes an instant abort (sub-second) from the ~15-30s no-first-move timeout
- Lichess challenge/decline params MUST be in the POST body (`application/x-www-form-urlencoded`), NOT the query string — Lichess ignores query-string params and silently makes a no-clock `correspondence` game (lichess-org/api issue #142). `LichessClient._request` form-encodes `form_params` into the body; `create_challenge` uses dotted keys `clock.limit`/`clock.increment`. The controller surfaces Lichess's assigned `speed` on `ChallengeSent`; the GUI logs it (e.g. "Challenged <opp> (rapid 300+3)") and warns loudly if a challenge came back `correspondence` (clock didn't land). `GameStarted` is logged with both colors + the game id for log correlation
- Lichess rated vs casual (RETRACTED as an abort cause 2026-06-28 — PROVEN cause is the challenger's `_cancel_all_pending` canceling its own just-accepted challenge; see the PROVEN-CAUSE bullet above; the user's manual web test of the same two accounts on the same two machines plays fine, falsifying same-owner/same-IP/casual-auto-abort. The Rated/Casual toggle + per-challenge logging below remain a useful knob/signal, NOT an abort fix): challenges send `rated=false` (casual) by default; `LICHESS_RATED=1` opts into `rated=true`, and at runtime a GUI "Rated: ON/OFF" toggle button (beside the time-control text in the Lichess panel, defaulting OFF) flips `self.lichess_rated` and calls `LichessController.set_rated`, so the NEXT manual or auto challenge sends the new mode without a restart. The mode is surfaced on `ChallengeSent.rated` (echoed from Lichess's challenge JSON, falling back to what we requested) and logged explicitly on every challenge line (e.g. `Challenged <opp> (blitz 180+2, casual, id=...)` / `..., rated, ...`) so the user can confirm at a glance which mode is sent — this directly answers the opponent's "are your challenges rated or casual?". The opponent (xmiao_ds) hypothesized Lichess "auto-aborts casual bot games at creation (documented behavior)" and now declines casual; that causal claim is UNDOCUMENTED — casual is a first-class supported bot mode (lichess-bot config lists `casual`), and the documented abort-before-first-move is a PLAYER-abort race (lichess-bot #125, lila #15318), not a server-side casual-auto-abort (our games abort server-side at ~0.5–1s with NO abort called by either side). So switching to rated is NOT a proven fix and is NOT blindly applied as the default; it is (a) a configurable knob and a logged signal, and (b) the DECISIVE experiment. **That experiment has now been RUN (game ENTGYOFG, 2026-06-28): a RATED challenge, on reportedly DIFFERENT public IPs, BOTH sides single-instance, and White (xmiao_ds) tried an instant book `e2e4` ~1s after start — the game STILL aborted at ~1s with `HTTP 400 "Not your turn, or game already over"` (0 moves, we were Black).** This FALSIFIES the opponent's "casual is auto-aborted, rated works normally" claim (a rated game aborted too) and FALSIFIES the no-first-move timeout (White tried to move instantly); same-IP is likely-falsified IF the two IPs are genuinely different (STILL UNVERIFIED — verify with `curl ifconfig.me` on EACH machine; the last "different machines" run was the same intranet = same public IP, RETRACTED). The ONLY hypothesis consistent with ALL the evidence (rated + different-IP + single-instance + instant-first-move, still aborts) is same-OWNER account-level anti-self-play (rated self-play forbidden since ~2016, lila #7031) — now the LEADING candidate, still NOT asserted as proven. The SOLE remaining decisive test is a THIRD-PARTY bot (a DIFFERENT owner) challenged from the same machine: if it PLAYS, same-owner is confirmed (no within-ToS way for one person's two bots to play each other — the fix is to play bots you don't own, not more code); if it ALSO aborts at ~1s/0 moves, the cause is xmiao_glm's setup, not the matchup. Do NOT assert any candidate as the cause (see the instant-abort bullet below for the full same-IP / same-owner / duplicate-stream analysis)
- Lichess outgoing-challenge lifecycle is surfaced: `challengeDeclined` and `challengeCanceled` event-stream events (previously silently dropped — the GUI log just stopped at "Challenged ...") now emit a `ChallengeDeclined(opponent, reason)` dataclass / a `Status`, and `_forget_outgoing` clears the `_pending_outgoing` entry (so a stale entry doesn't wrongly decline a reverse challenge or block the auto-loop). The GUI logs `Challenge to <opp> was DECLINED (reason: ...)` and, if the reason is `casual`, adds a hint to set `LICHESS_RATED=1`. This made the 2026-06-28 decline (xmiao_ds declined xmiao_glm's casual challenge, id=4biqVLpL) visible instead of silent — and that run is meaningful: the instant-at-creation abort did NOT occur (the challenge was received, evaluated, and explicitly *declined* before any game was created), so the decline path is distinct from the accept→abort path and tells us nothing new about the abort cause
- Lichess bidirectional double-game guard: if we have a pending outgoing challenge to a peer, their near-simultaneous reverse challenge is auto-declined with reason `later` (accepting would start a second game where each bot can latch a different one and both appear to wait on the other). Without a pending outgoing, normal peer challenges auto-accept as usual
- Lichess first-move budget: the bot's first move of a game is capped at `FIRST_MOVE_BUDGET_MS` (1000ms) so it lands before the opponent can abort (Lichess disallows a single-player abort once a move is on the board); later moves use the normal clock-derived budget (`remaining//20 + inc//2`, capped 200–5000ms). The GUI logs `Engine thinking...` (from the `Status` event) before each move so an abort during the think is visibly our-side-trying, not a freeze
- Lichess instant opening-book move 1 (Experiment A — TESTED & FALSIFIED as a workaround; RETRACTED as an abort fix 2026-06-28 — the abort was the challenger canceling its own just-accepted challenge, NOT a moves-on-board race; see the PROVEN-CAUSE bullet above. With that cancel fixed the game no longer aborts at creation, so this is no longer needed to beat the abort; retained as a toggle/diagnostic): when the bot is WHITE and it is move 1 from `STARTING_FEN`, `_maybe_move` posts an instant book move from `_OPENING_BOOK_WHITE_MOVE1` (`("e2e4",)`) with NO engine think (`_instant_first_move` verifies legality via `generate_legal_moves` first). The `Status` is `"Playing opening book (e2e4)..."` (the GUI sets `engine_started` for any Status starting with `"Engine thinking"` OR `"Playing opening book"`). Originally aimed to land move 1 at ~POST-RTT ahead of a same-owner creation abort (~0.5–1s) IF the abort respects moves-on-board. **Tested on BOTH sides, both colors, both casual AND rated (game CTteTHAU 2026-06-28: xmiao_ds White played an instant `e2e4`, 0 ms think; game ls5G7MOe 2026-06-28: xmiao_glm White played an instant `e2e4`, 0 ms think; game ENTGYOFG 2026-06-28: xmiao_ds White tried an instant `e2e4` under RATED + reportedly-different-IP conditions) — ALL still aborted with `HTTP 400 "game already over"` (`"Not your turn, or game already over"`); the abort beat even the fastest possible first move in either color and either mode.** So Experiment A is FALSIFIED as a workaround — the abort does not respect an incoming move. The failure is technically ambiguous (the no-keep-alive `urllib` POST RTT ~0.5–1s vs the abort ignoring moves-on-board), but a keep-alive POST refactor (A2) was rejected because the opponent's keep-alive lichess-bot POST already lost the same race. The mechanism is retained only as a toggleable diagnostic / fast move-1 if conditions ever change; set `_OPENING_BOOK_WHITE_MOVE1 = ()` to disable. It applies only when we are White (when we are Black the opponent, White, must move first). No code fix on either side reliably stops the abort; the cause is server-side — same-IP and/or same-owner, both still UNVERIFIED (an earlier "same-IP ruled out by ls5G7MOe" is RETRACTED: that run's two machines were on the same intranet = same public IP, so it did not change the IP Lichess sees). Decisive tests: (A) two own bots on genuinely different public IPs — one on a phone cellular hotspot (different carrier IP), not a VPN (Lichess flags VPN IPs) — if it plays, same-IP was the cause and the two own-bots CAN play via different IPs; if it aborts, same-owner is the cause; (B) challenge a third-party bot (a DIFFERENT owner) from the same machine — if it plays, same-owner was the cause
- Lichess instant-abort-at-creation diagnosis — PROVEN ROOT CAUSE 2026-06-28 (see the PROVEN-CAUSE bullet above): the abort was the CHALLENGER's `_cancel_all_pending()` canceling its own just-accepted challenge (challenge id == game id), NOT same-IP/owner/duplicate-stream (all RETRACTED — the user's manual web test of the same two accounts on the same two machines plays fine). The diagnostics below (`_log_abort_diagnostic`, gameFull-status, OPEN/CLOSE counter, singleton lock) remain useful observability but name NO proven cause. Historical note: when the gameFull arrives already carrying an `aborted`/`noStart` status, `_process_game_stream` skips `_maybe_move` (so no `Engine thinking...`/`Playing opening book...` Status is ever pushed) and the GUI logs `GameStarted` then `GameFinished` with 0 moves (in EITHER color — we move first as White, or wait on White as Black). `noStart` (aborted BEFORE it started — a creation-time conflict) is distinct from `aborted` (a player/no-first-move abort); both render as "Game aborted", so the controller surfaces the ACTUAL status + the gameFull's `source`/`speed`/`variant`/titles in a summary `Status` (`_log_already_over_game_full`) and logs the full gameFull JSON (`logger.warning`) — Lichess gives no explicit "who aborted" field, so the status + context is all we have. The GUI tracks `engine_started` on the live game (set True by an `Engine thinking...`/`Playing opening book...` Status or `EngineMoved`); `_log_abort_diagnostic` (fired for both `aborted` and `noStart` via `ABORT_LIKE_STATUSES`) tells the regimes apart by COLOR: **White** uses `engine_started` (never started thinking ⇒ already over when we connected; started thinking ⇒ opponent aborted during our think); **Black** uses elapsed in THREE regimes (we never start thinking while Black, so `engine_started` is always False and useless — only elapsed distinguishes them): `< INSTANT_ABORT_THRESHOLD_S` (5s) ⇒ instant abort at creation (duplicate event-stream conflict); `5s–NO_FIRST_MOVE_WARN_S` (20s) ⇒ a MID-THINK abort — the game was LIVE then aborted before White's first move, too soon for the ~15-30s timeout (the 20s stall warning never fired), so it's one account connecting to the game stream twice (a second GUI window / lichess-bot for our account alongside the GUI / opponent running two bot processes) or a manual Abort click — a single instance can't cause this (`gameStart` is deduped, `abort()` is only the manual button); `≥ 20s` ⇒ the genuine no-first-move timeout ("opponent not playing"). The instant and mid-think messages explicitly note the opponent may have been trying to move (its move landed on an already-dead game → `HTTP 400: game already over`), so neither says "opponent not playing" (reserved for the genuine timeout). The event-stream dedup branch logs a `Status` note on a duplicate `gameStart` (a second event-stream connection for this account — the same signature). Formerly the leading candidate, now DEMOTED to WEAKENED: a DUPLICATE event-stream connection on one of the accounts: two LIVE streams on one account (two bot processes, OR one process with a stale/lingering reconnect stream Lichess hasn't closed) both receive the gameStart (Lichess pushes it to EVERY live stream on the account) and both connect to the same game's `/api/bot/game/stream/{id}`, and Lichess aborts the conflict at creation — this happens whether our one challenge was received once or twice (the gameStart, NOT the challenge, is what double-connects the game stream). This is WEAKENED, not proven: BOTH sides were confirmed single-instance across NSyfD5g5, CTteTHAU, ls5G7MOe, and pJiIjxtm (singleton lock + one PID + one challenge id + single receipt/accept), so a two-process duplicate is largely ruled out. The `_log_abort_diagnostic` GUI message now names NO proven cause and leads with the live UNVERIFIED server-side candidates (same-PUBLIC-IP and same-OWNER) plus the decisive checks: run `curl ifconfig.me` on EACH machine to compare public IPs, challenge a THIRD-PARTY bot (different owner) from the same machine, run the two bots on DIFFERENT public IPs (cellular hotspot, not a VPN). Do NOT assert any candidate as the cause. RETRACTED: the earlier claim that 'the reliable proof of which side has the duplicate is the `Connected as <user>` line COUNT in each log' is FALSE — lichess-bot logs 'Connected' on each reconnect (`start()` re-runs when the event stream drops), so TWO `Connected as <user>` lines can be ONE process, not two. Game NSyfD5g5 (2026-06-27): our side clean (one `Connected as xmiao_glm`, one challenge id, gameFull already aborted, never started thinking, we were White), opponent xmiao_ds logged the challenge ONCE + accepted ONCE yet STILL aborted at creation (turn=W, 0 moves, 0.0s); xmiao_ds's TWO `Connected as xmiao_ds` lines are reconnect logging, NOT proof of a duplicate on xmiao_ds. The decisive check is the bot PROCESS count on BOTH machines (Task Manager), not the `Connected` line count and not the receipt count; double challenge receipt (opponent receiving it twice) corroborates two LIVE streams but not two processes. The fix is to run only ONE bot process per account (a second process for EITHER account double-connects and aborts every game); if a side has only one process yet aborts recur, its stream may be reconnecting/stale (network/proxy idle-timeout) — run that bot in this GUI too (one clean connection, `gameStart` deduped). Decisive experiment: run BOTH bots in this GUI (one event stream each); if aborts stop -> lichess-bot/stale stream was it; if they continue -> it's the Lichess matchup (same-OWNER and/or same-PUBLIC-IP — unverified abort policy; an earlier "same-IP ruled out by ls5G7MOe" is RETRACTED: that run's two machines were on the same intranet = same public IP, so it did not change the IP Lichess sees — test same-IP with genuinely different public IPs, e.g. a phone cellular hotspot, not a VPN) or a Lichess-side abort, not our code. `_start_lichess` stops any running controller before creating a new one, so re-entering Lichess mode does not leak a second live event-stream thread on our side. NOTE: `_process_game_stream` returns as soon as the game is over, so a trailing `gameState` carrying the same `aborted` status no longer pushes a SECOND `GameFinished` (game ENTGYOFG 2026-06-28 had logged "Game over: Game aborted" + the diagnostic TWICE from a gameFull+gameState pair); the GUI's `GameFinished` handler also guards against re-logging/re-diagnosing an already-over game. `_log_abort_diagnostic` now emits a concise factual HEADLINE first — `Abort: N move(s), we were <color> [rated|casual], status '<status>' <dur>. Cause below.` — before the detailed wall-of-text analysis, so what happened is readable at a glance and in Copy Log (the panel truncates the detail to 30 chars). The `[rated]`/`[casual]` tag comes from `self.lichess_last_rated`, set on `ChallengeSent` and also shown on the `GameStarted` log line — surfacing the mode is the key bit, since ENTGYOFG was a RATED game that still aborted
- Lichess matching diagnostics (opponent's `xmiao_glm.md` collaborative-diagnostics request, 2026-06-28): three additive `Status` diagnostics on OUR side so the two bots' timestamped logs correlate and a duplicate-stream / duplicate-process is PROVABLE or REFUTABLE from our own log. They do NOT assert any abort cause (the same-PUBLIC-IP / same-OWNER candidates stay UNVERIFIED). (1) **Game-stream OPEN/CLOSE counter** — `_process_game_stream` is wrapped in try/finally; `_game_stream_open` pushes `Game stream OPEN id=<gid[:8]> n=<n>` at the top (incrementing `self._stream_count[gid]` under `_state_lock`) and `_game_stream_close` pushes `Game stream CLOSE id=<gid[:8]> n=<n>` in the finally (decrementing; balances every exit path — normal end, early game-over `return True`, or a raised exception, so a leaked stream is visible). `n=1` is the normal single stream; `n>1` pushes an extra `DUPLICATE game stream for <gid> (n=N)!` Status — a second event-stream connection on this account double-connected the game stream (the instant-at-creation abort signature); this is the single most useful diagnostic. (2) **gameFull status on arrival** — every gameFull pushes `gameFull: status=<status>, White=<w>, Black=<b> (LIVE|DEAD)` before `GameStarted` (`_format_game_full_status`); `LIVE` = status in `ACTIVE_STATUSES` (`started`/`created`), `DEAD` = `aborted`/`noStart` (the instant-abort-at-creation signature, distinguishable from a game that was LIVE then aborted mid-think). (3) **Per-account singleton lock + PID** — `start()` acquires a SOFT per-account localhost socket lock (`_acquire_singleton` → `_default_singleton_bind` binds a deterministic port via `hashlib.md5(username.lower())` in the registered range `SINGLETON_PORT_BASE..+SINGLETON_PORT_RANGE`; `SO_REUSEADDR` is deliberately NOT set so Windows can't port-share and defeat the lock). On success the connect line is `Connected as <name> (PID <pid>, single instance)`; on failure `Connected as <name> (PID <pid>) — DUPLICATE: another process is already running this account...` + an `Error`. SOFT/non-trapping: a duplicate is logged + flagged, NEVER blocks (a transient bind collision must not brick the GUI). The bind is INJECTABLE (`singleton_bind: Callable[[str], bool] = None` in `__init__`; tests pass a fake so they never open real sockets, and `make_controller` injects `lambda name: True`); `stop()` closes the socket. The port is deterministic per username so two processes for the SAME account collide (detected) while our own two bots (different usernames) get different ports and coexist. `hash()` is NOT used (it is randomized per process and would defeat the lock). NOTE: this singleton lock on OUR side is NEW (added 2026-06-28); the prior 'BOTH sides confirmed single-instance' confirmations (NSyfD5g5/CTteTHAU/ls5G7MOe/pJiIjxtm) relied on the opponent's lichess-bot single-instance + our `Connected as ... (PID ...)` line, NOT a lock on our side — the lock now makes a second OUR-side process self-evident in our own log. The decisive check remains the bot PROCESS count on BOTH machines (Task Manager), not any single log line